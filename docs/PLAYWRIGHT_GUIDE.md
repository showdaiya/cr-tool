# Playwright E2Eテスト ガイド

## 目次

1. [Playwrightとは](#playwrightとは)
2. [インストール方法](#インストール方法)
3. [基本的なコマンド一覧](#基本的なコマンド一覧)
4. [設定ファイルの解説](#設定ファイルの解説)
5. [テストの書き方](#テストの書き方)
6. [codegen（コード自動生成）](#codegenコード自動生成)
7. [デバッグ方法](#デバッグ方法)
8. [CI/CD連携](#cicd連携)
9. [トラブルシューティング](#トラブルシューティング)

---

## Playwrightとは

### 概要

Playwright は **Microsoft** が開発したE2E（End-to-End）テストフレームワークです。

### E2Eテストとは？

```
┌─────────────────────────────────────────────────────────────┐
│                    テストの種類                              │
├─────────────────────────────────────────────────────────────┤
│  単体テスト (Unit Test)                                      │
│  └── 関数やコンポーネント単位でテスト                         │
│  └── 例: 「この関数に2を渡すと4が返る」                       │
│                                                              │
│  結合テスト (Integration Test)                               │
│  └── 複数のモジュールを組み合わせてテスト                     │
│  └── 例: 「APIとDBが正しく連携する」                          │
│                                                              │
│  E2Eテスト (End-to-End Test)  ← Playwrightはここ            │
│  └── 実際のブラウザでユーザー操作をシミュレート               │
│  └── 例: 「ボタンをクリックしたら画面が切り替わる」           │
└─────────────────────────────────────────────────────────────┘
```

### なぜPlaywrightを選ぶのか？

| 特徴 | Playwright | Selenium | Cypress |
|------|------------|----------|---------|
| 開発元 | Microsoft | OpenJS Foundation | Cypress.io |
| 対応ブラウザ | Chrome, Firefox, Safari, Edge | 全ブラウザ | Chrome, Firefox, Edge |
| 速度 | ⚡ 高速 | 遅い | 中程度 |
| 自動待機 | ✅ あり | ❌ 手動 | ✅ あり |
| モバイル対応 | ✅ エミュレーション | ❌ 別ツール必要 | ❌ 別ツール必要 |
| 並列実行 | ✅ 標準対応 | △ 設定必要 | ❌ 有料 |
| コード生成 | ✅ codegen | ❌ なし | ❌ なし |
| 言語 | JS/TS/Python/C#/Java | 多言語 | JSのみ |

**結論**: モダンなWeb開発には Playwright が最適

---

## インストール方法

### 前提条件

- Node.js 18以上
- npm または yarn

### 手順

```powershell
# プロジェクトディレクトリに移動
cd your-project

# Playwrightを初期化（対話形式）
npm init playwright@latest
```

### 対話での選択肢

```
? Where to put your end-to-end tests?
  → e2e （テストファイルの保存先）

? Add a GitHub Actions workflow?
  → Yes （CI/CDで自動テストしたい場合）

? Install Playwright browsers?
  → Yes （テスト用ブラウザをダウンロード）
```

### インストールされるもの

| ファイル/フォルダ | 役割 |
|------------------|------|
| `playwright.config.ts` | 設定ファイル |
| `e2e/` | テストファイルを置くフォルダ |
| `e2e/example.spec.ts` | サンプルテスト |
| `.github/workflows/playwright.yml` | GitHub Actions設定 |
| `node_modules/@playwright/test` | テストランナー |

### ブラウザのダウンロード先

```
Windows: C:\Users\{ユーザー名}\AppData\Local\ms-playwright\
Mac/Linux: ~/Library/Caches/ms-playwright/
```

約300〜500MBのディスク容量が必要です。

---

## 基本的なコマンド一覧

### テスト実行

```powershell
# 全テスト実行
npx playwright test

# 特定ファイルのみ実行
npx playwright test e2e/home.spec.ts

# 特定のテスト名で絞り込み
npx playwright test -g "ページタイトル"

# 特定ブラウザのみ
npx playwright test --project=chromium
npx playwright test --project=firefox
npx playwright test --project=webkit
```

### UIモード

```powershell
# 視覚的なUIでテスト実行（推奨）
npx playwright test --ui
```

UIモードでできること:
- テストの一覧表示
- 個別テストの実行
- 失敗箇所のスクリーンショット確認
- ステップごとの実行

### デバッグモード

```powershell
# ブラウザを表示しながらステップ実行
npx playwright test --debug

# 特定テストをデバッグ
npx playwright test e2e/home.spec.ts --debug
```

### コード生成

```powershell
# 操作を記録してコード生成
npx playwright codegen http://localhost:3000

# スマホエミュレーションで生成
npx playwright codegen --device="iPhone 12" http://localhost:3000

# Firefoxで生成
npx playwright codegen --browser=firefox http://localhost:3000
```

### レポート確認

```powershell
# HTMLレポートを開く
npx playwright show-report

# レポートは playwright-report/ フォルダに生成される
```

### ブラウザ管理

```powershell
# ブラウザを再インストール
npx playwright install

# 特定ブラウザのみインストール
npx playwright install chromium
npx playwright install firefox
npx playwright install webkit

# インストール済みブラウザ確認
npx playwright --version
```

---

## 設定ファイルの解説

### `playwright.config.ts` の構造

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 基本設定
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  
  testDir: './e2e',
  // テストファイルを探すディレクトリ
  
  fullyParallel: true,
  // true: 全テストを並列実行（高速）
  // false: 順番に実行
  
  forbidOnly: !!process.env.CI,
  // CI環境で test.only() を禁止
  // （本番で特定テストだけ実行されるミスを防ぐ）
  
  retries: process.env.CI ? 2 : 0,
  // 失敗時のリトライ回数
  // ローカル: 0回（即失敗）
  // CI: 2回（フレーキーテスト対策）
  
  workers: process.env.CI ? 1 : undefined,
  // 並列ワーカー数
  // ローカル: undefined（CPU数に応じて自動）
  // CI: 1（安定性重視）
  
  reporter: 'html',
  // レポート形式
  // 'html': HTMLファイル
  // 'list': コンソール出力
  // 'dot': 簡易表示
  
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 共通オプション（全プロジェクトに適用）
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  
  use: {
    baseURL: 'http://localhost:3000',
    // page.goto('/') → http://localhost:3000/ になる
    
    trace: 'on-first-retry',
    // トレース（操作記録）をいつ取るか
    // 'on': 常に
    // 'off': 取らない
    // 'on-first-retry': 最初のリトライ時（デバッグ用）
    
    screenshot: 'only-on-failure',
    // スクリーンショットをいつ撮るか
    // 'on': 常に
    // 'off': 撮らない
    // 'only-on-failure': 失敗時のみ
    
    video: 'retain-on-failure',
    // 動画をいつ撮るか
    // 'on': 常に
    // 'off': 撮らない
    // 'retain-on-failure': 失敗時のみ保持
  },
  
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // プロジェクト（ブラウザ/デバイス設定）
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  
  projects: [
    // デスクトップブラウザ
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
    
    // モバイルエミュレーション
    {
      name: 'Mobile Chrome',
      use: { ...devices['Pixel 5'] },
    },
    {
      name: 'Mobile Safari',
      use: { ...devices['iPhone 12'] },
    },
  ],
  
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 開発サーバー設定
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  
  webServer: {
    command: 'npm run dev',
    // テスト前に実行するコマンド
    
    url: 'http://localhost:3000',
    // このURLが応答するまで待機
    
    reuseExistingServer: !process.env.CI,
    // ローカル: 既存サーバーを再利用
    // CI: 新規起動
    
    timeout: 120 * 1000,
    // 起動タイムアウト（ミリ秒）
  },
});
```

### 利用可能なデバイス一覧

```typescript
// 主要なデバイス例
devices['Desktop Chrome']
devices['Desktop Firefox']
devices['Desktop Safari']
devices['Desktop Edge']
devices['Pixel 5']
devices['Pixel 7']
devices['iPhone 12']
devices['iPhone 13']
devices['iPhone 14']
devices['iPad Pro 11']
devices['Galaxy S8']
devices['Galaxy Tab S4']

// 全リストを確認
// npx playwright open --device-list
```

---

## テストの書き方

### 基本構造

```typescript
import { test, expect } from '@playwright/test';

// テストスイート（グループ）
test.describe('機能名', () => {
  
  // 各テスト前に実行
  test.beforeEach(async ({ page }) => {
    await page.goto('/');
  });
  
  // 各テスト後に実行
  test.afterEach(async ({ page }) => {
    // クリーンアップ処理
  });
  
  // 個別テスト
  test('テスト名', async ({ page }) => {
    // テスト内容
  });
  
});
```

### ページ操作

```typescript
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ナビゲーション
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await page.goto('/');                    // URLに移動
await page.goto('/about');               // 相対パス
await page.goBack();                     // 戻る
await page.goForward();                  // 進む
await page.reload();                     // リロード

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// クリック
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await page.click('button');              // CSSセレクタ
await page.click('text=送信');           // テキストで指定
await page.getByRole('button', { name: '送信' }).click();  // 推奨
await page.getByTestId('submit-btn').click();  // data-testid

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 入力
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await page.fill('input[name="email"]', 'test@example.com');
await page.getByLabel('メールアドレス').fill('test@example.com');
await page.getByPlaceholder('入力してください').fill('テスト');

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 選択
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await page.selectOption('select', 'option1');        // value
await page.selectOption('select', { label: '選択肢1' }); // ラベル
await page.getByRole('combobox').selectOption('option1');

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// チェックボックス/ラジオ
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await page.check('input[type="checkbox"]');
await page.uncheck('input[type="checkbox"]');
await page.getByLabel('同意する').check();

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 待機
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await page.waitForTimeout(1000);         // 1秒待機（非推奨）
await page.waitForSelector('.loading');  // 要素が出現するまで
await page.waitForLoadState('networkidle'); // ネットワーク静止まで
```

### 要素の取得方法（Locator）

```typescript
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 推奨: ユーザー視点のLocator
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

page.getByRole('button', { name: '送信' })  // ロール + 名前
page.getByLabel('ユーザー名')                // ラベル
page.getByPlaceholder('入力してください')    // プレースホルダー
page.getByText('ようこそ')                   // テキスト
page.getByTestId('login-form')              // data-testid属性

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// CSSセレクタ（非推奨だが便利）
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

page.locator('.btn-primary')
page.locator('#submit-button')
page.locator('div.card > h2')

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 複合
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

page.locator('article').filter({ hasText: 'Playwright' })
page.locator('.card').first()
page.locator('.card').nth(2)  // 3番目（0始まり）
page.locator('.card').last()
```

### アサーション（検証）

```typescript
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// ページ全体
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await expect(page).toHaveTitle('タイトル');
await expect(page).toHaveTitle(/部分一致/);
await expect(page).toHaveURL('/dashboard');
await expect(page).toHaveURL(/\/dashboard/);

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 要素の状態
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await expect(locator).toBeVisible();      // 表示されている
await expect(locator).toBeHidden();       // 非表示
await expect(locator).toBeEnabled();      // 有効
await expect(locator).toBeDisabled();     // 無効
await expect(locator).toBeChecked();      // チェック済み
await expect(locator).toBeFocused();      // フォーカス中

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 要素のテキスト/属性
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await expect(locator).toHaveText('テキスト');
await expect(locator).toContainText('部分');
await expect(locator).toHaveAttribute('href', '/about');
await expect(locator).toHaveClass(/active/);
await expect(locator).toHaveValue('入力値');

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// 要素の数
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

await expect(locator).toHaveCount(3);     // 3個ある
```

### 実際のテスト例

```typescript
import { test, expect } from '@playwright/test';

test.describe('ログイン機能', () => {
  
  test('正常にログインできる', async ({ page }) => {
    // 1. ログインページに移動
    await page.goto('/login');
    
    // 2. フォームに入力
    await page.getByLabel('メールアドレス').fill('user@example.com');
    await page.getByLabel('パスワード').fill('password123');
    
    // 3. ログインボタンをクリック
    await page.getByRole('button', { name: 'ログイン' }).click();
    
    // 4. ダッシュボードに遷移したことを確認
    await expect(page).toHaveURL('/dashboard');
    await expect(page.getByText('ようこそ')).toBeVisible();
  });
  
  test('パスワードが間違っているとエラーが表示される', async ({ page }) => {
    await page.goto('/login');
    await page.getByLabel('メールアドレス').fill('user@example.com');
    await page.getByLabel('パスワード').fill('wrong-password');
    await page.getByRole('button', { name: 'ログイン' }).click();
    
    // エラーメッセージが表示されることを確認
    await expect(page.getByText('パスワードが正しくありません')).toBeVisible();
  });
  
});
```

---

## codegen（コード自動生成）

### 概要

`codegen` は、ブラウザ操作を記録してテストコードを自動生成するツールです。

### 起動方法

```powershell
# 基本
npx playwright codegen http://localhost:3000

# スマホエミュレーション
npx playwright codegen --device="iPhone 12" http://localhost:3000

# Firefoxで起動
npx playwright codegen --browser=firefox http://localhost:3000

# ファイルに直接出力
npx playwright codegen -o e2e/my-test.spec.ts http://localhost:3000
```

### 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│  操作用ブラウザ                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                                                      │    │
│  │      ここで実際に操作する                            │    │
│  │      クリック、入力、スクロールなど                   │    │
│  │                                                      │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  Playwright Inspector（コード生成ウィンドウ）                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  import { test, expect } from '@playwright/test';    │    │
│  │                                                      │    │
│  │  test('test', async ({ page }) => {                  │    │
│  │    await page.goto('http://localhost:3000/');        │    │
│  │    await page.getByRole('button').click();           │    │
│  │    await page.getByLabel('名前').fill('テスト');     │    │
│  │  });                                                 │    │
│  │                                                      │    │
│  └─────────────────────────────────────────────────────┘    │
│  [Copy]  [Record]  [Step Over]  [Resume]                    │
└─────────────────────────────────────────────────────────────┘
```

### ツールバーの説明

| ボタン | 機能 |
|--------|------|
| 🔴 Record | 記録開始/停止 |
| ⏸ Pause | 一時停止 |
| ▶ Resume | 再開 |
| ⏭ Step Over | 1ステップ実行 |
| 📋 Copy | コードをクリップボードにコピー |
| 🎯 Pick Locator | 要素のセレクタを調査 |

### 使い方の流れ

1. **codegenを起動**
   ```powershell
   npx playwright codegen http://localhost:3000
   ```

2. **ブラウザで操作**
   - ボタンをクリック
   - フォームに入力
   - リンクをクリック
   - など

3. **コードが自動生成される**
   - Inspector ウィンドウにコードが表示される

4. **コードをコピー**
   - 「Copy」ボタンでクリップボードにコピー

5. **テストファイルに貼り付け**
   - `e2e/xxx.spec.ts` にペースト

### Pick Locator 機能

要素のセレクタを調べるのに便利です。

1. ツールバーの「Pick Locator」（🎯）をクリック
2. 調べたい要素にマウスを乗せる
3. セレクタがハイライト表示される
4. クリックするとセレクタがコピーされる

---

## デバッグ方法

### 方法1: `--debug` オプション

```powershell
npx playwright test --debug
```

- ブラウザが表示された状態でステップ実行
- Inspector ウィンドウで操作可能
- ブレークポイントで停止

### 方法2: `--ui` オプション

```powershell
npx playwright test --ui
```

- GUIでテストを管理
- 失敗箇所のスクリーンショット確認
- 個別テストの実行

### 方法3: コード内で `page.pause()`

```typescript
test('デバッグ用', async ({ page }) => {
  await page.goto('/');
  
  // ここで一時停止（Inspector が開く）
  await page.pause();
  
  await page.click('button');
});
```

### 方法4: トレースビューア

```powershell
# テスト実行時にトレースを記録
npx playwright test --trace on

# トレースを確認
npx playwright show-trace test-results/xxx/trace.zip
```

トレースで確認できること:
- 各ステップのスクリーンショット
- ネットワークリクエスト
- コンソールログ
- DOMスナップショット

---

## CI/CD連携

### GitHub Actions

`npm init playwright@latest` で自動生成される `.github/workflows/playwright.yml`:

```yaml
name: Playwright Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    
    - name: Install dependencies
      run: npm ci
    
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    
    - name: Run Playwright tests
      run: npx playwright test
    
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
```

### テスト結果の確認

1. GitHub の Actions タブを開く
2. 該当の Workflow を選択
3. 「playwright-report」をダウンロード
4. 解凍して `index.html` を開く

---

## トラブルシューティング

### ブラウザが起動しない

```powershell
# ブラウザを再インストール
npx playwright install

# 依存関係も含めてインストール（Linux）
npx playwright install --with-deps
```

### テストがタイムアウトする

```typescript
// タイムアウトを延長
test('長いテスト', async ({ page }) => {
  test.setTimeout(60000); // 60秒
  // ...
});

// 設定ファイルで全体に適用
export default defineConfig({
  timeout: 60000,
});
```

### 要素が見つからない

```typescript
// 待機時間を延長
await expect(locator).toBeVisible({ timeout: 10000 });

// 要素が読み込まれるまで待機
await page.waitForSelector('.my-element');

// ネットワークが落ち着くまで待機
await page.waitForLoadState('networkidle');
```

### CI でのみ失敗する

```typescript
// CI環境かどうかで処理を分ける
if (process.env.CI) {
  // CI用の処理
}

// 設定ファイルでリトライを増やす
retries: process.env.CI ? 3 : 0,
```

### スクリーンショットが真っ白

```typescript
// ページの読み込み完了を待つ
await page.waitForLoadState('domcontentloaded');

// または
await page.waitForTimeout(1000); // 最終手段
```

---

## 参考リンク

- [Playwright 公式ドキュメント](https://playwright.dev/docs/intro)
- [Playwright GitHub](https://github.com/microsoft/playwright)
- [Locator API リファレンス](https://playwright.dev/docs/locators)
- [Assertions リファレンス](https://playwright.dev/docs/test-assertions)

---

*最終更新: 2026-01-03*
